import SafetyPage, { metadata as safetyMetadata } from '../safety/page';

export const metadata = {
  ...safetyMetadata,
  title: 'Community Guidelines | Free Random Video Chat',
};

export default SafetyPage;
